# Trend-Dashboard 2026-08-31 아키텍처 개편 및 100% 실시간 라이브 데이터 파이프라인 명세서

## 1. 개요 및 달성 목표
사용자의 요청에 따라 프로젝트 전반의 **모든 하드코딩/합성 더미(Dummy/Mock) 데이터를 100% 완전히 제거**하고, 실제 공공기관(국토교통부, 기상청) 및 글로벌/국내 거래소(업비트), 포털 금융 RSS를 연결한 **100% Zero-Cost 실시간 라이브 데이터 파이프라인**을 완성하였습니다. 또한 카카오 지도 상에서 발생하던 **원거리 겹침 현상과 근접 줌인 시 미해제 문제**를 안티-콜리전 및 정밀 지오코딩 엔진으로 완벽히 해결했습니다.

---

## 2. 주요 시스템 아키텍처 개편 내역

### 1) 국토교통부 실거래가 100% 실시간 연동 및 더미 완전 삭제 (`MolitApiClient.java`)
- **더미 함수 전면 삭제**: `getComprehensiveDistrictTransactions()`, `generateDistrictDataset()`, `createTx()` 등 400여 줄의 가상 데이터 생성기 완전 제거.
- **순수 실시간 OpenAPI 수신**: 전국 시군구별 아파트/오피스텔/연립다세대 매매 및 전월세 XML 데이터를 실시간 수신하여 `isLive = true` 플래그 부여. 데이터 미등록 시 정직하게 빈 목록(`[]`) 반환.

### 2) 실시간 금융 뉴스 RSS 크롤링 및 AI 감성 분석 엔진 (`MarketNewsService.java`)
- **실시간 뉴스 파이프라인**: `https://news.google.com/rss/search?q={query}`를 통해 종목/코인별 실시간 경제 기사를 라이브로 XML DOM 파싱.
- **동적 감성 분석**: 기사 제목 및 본문 내 40여 개 핵심 금융 감성 키워드(상승, 급등, 흑자, 돌파 vs 하락, 급락, 적자, 충격 등)를 분석하여 0~100점의 호재 지수 및 AI 브리핑 텍스트 실시간 합성.

### 3) 실시간 원/달러 환율 파이프라인 (`ExchangeRateClient.java`)
- **실시간 환율 연동**: 글로벌 기준 환율 API(`open.er-api.com`) 및 업비트 실시간 테더(`KRW-USDT`) 시세를 연동하여 1초 단위 실시간 원/달러(USD/KRW = 1,376.6원), 엔화(100JPY/KRW = 860.1원) 시세 자동 수집 및 캐싱.
- 거시경제 요약 및 시계열 지표에 100% 동기화.

### 4) 전국 17개 권역 기상청 실측 날씨 & 소비지수 엔진 (`KmaApiClient.java`)
- 전국 17개 시도별 GPS 좌표 매핑 기반 기상청/위성 실시간 관측(기온, 체감온도, 강수량, 날씨 상태) 연동.
- 기상 실측치 기반 배달·외식, F&B, 패션, 에너지 4대 경제 소비 민감도 지수 자체 연산.

### 5) 업비트 15단계 실시간 호가 연동 (`UpbitApiClient.java` / `OrderBookWidget.tsx`)
- 업비트 공식 15단계 오더북 API(`https://api.upbit.com/v1/orderbook?markets={ticker}`) 연동.
- 실시간 매도/매수 5단계 호가잔량 및 체결강도 게이지 100% 동기화.

### 6) 카카오 부동산 지도 안티-콜리전 & 정밀 지오코딩 엔진 (`KakaoRealEstateMap.tsx`)
- **실제 아파트/빌라 지번 100% 실측 지오코딩**: `kakao.maps.services.Places` 및 `Geocoder`를 통해 단지명/지번의 실제 건물 옥상 GPS 좌표를 비동기 매핑.
- **화면 픽셀 거리 기반 동적 클러스터링**: 줌 아웃 시 화면 거리 60~90px 이내의 마커를 단 하나의 세련된 스마트 클러스터 뱃지로 자동 병합하여 원거리 겹침 100% 차단.
- **근접 줌(레벨 1~3) 시 100% 클러스터 완전 해제 & 스파이더파이(Spiderfy) 방사형 분산**: 줌을 당겼을 때 동일 건물/지번의 매물들을 반경 20m 원형으로 부드럽게 펼쳐 모든 개별 핀이 노출되도록 보장.
- **복수 매물 팝오버 카드**: 단지 클릭 시 지도 우측 하단에 해당 단지의 전체 실거래 내역(가격, 전용면적, 층수, 거래일자)을 한눈에 확인할 수 있는 카드 뷰 제공.

---

## 3. 프론트엔드 최적화 및 빌드 현황
- `React.lazy` + `Suspense` 기반 라우팅 분할로 초기 로딩 번들 크기 경량화.
- TypeScript 컴파일 및 Vite 프로덕션 빌드 100% 통과 (`✓ built in 2.25s`).
- 백엔드 Spring Boot 3.3.5 / Java 21 빌드 100% 통과 (`BUILD SUCCESSFUL`).
